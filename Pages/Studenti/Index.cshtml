@page
@model GrozaEsteraDaniela_proiect6.Pages.Studenti.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Student[0].Nume_Prenume)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Student[0].Nr_matricol)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Student[0].DataNasterii)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Student) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nume_Prenume)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Nr_matricol)
            </td>
                <td>
                    @Html.DisplayFor(modelItem => item.DataNasterii)
                </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
